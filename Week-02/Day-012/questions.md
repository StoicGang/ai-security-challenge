# Similar Questions

This file contains the actual output produced by the **Day 012 - RAG Q&A** implementation. It documents the question, the retrieved context, and the grounded answer generated by the RAG pipeline.

---

## Question

> What is the formula for the FGSM? What each field tells?

---

## Retrieved Context

> One line of math. Four moving parts. A model that breaks in a single step.
>
> In the last article, we established what Fast Gradient Sign Method (FGSM) is and why it exists.
>
> Now we go inside it.
>
> By the end of this article, you should be able to look at the FGSM formula, point at each piece, and explain in plain language what it is doing and why it matters. No ML background required.
>
> **The Formula**
>
> Here it is. Exactly as Goodfellow wrote it in 2014:
>
> ```
> x_adv = x + ε · sign(∇ₓ J(θ, x, y))
> ```
>
> **Part 1: x, The Input**
>
> `x` is the clean input. An image, a file, or any untampered data point. This is where the attack starts.
>
> **Part 2: ∇ₓJ(θ, x, y), The Gradient**
>
> This is the compass. `J(θ, x, y)` is the loss function that measures how wrong the model's prediction is.
>
> ---
>
> The retrieved context also included the article's concluding section explaining FGSM's limitations and mentioning BIM and PGD as iterative improvements.

---

## Grounded Answer

> I don't know based on the provided context.

---

## Observation

The RAG pipeline executed successfully end to end:

- The question was converted into an embedding.
- The most relevant chunks were retrieved from ChromaDB.
- The retrieved context was passed to the Gemini model.
- The LLM correctly refused to fabricate information because the retrieved context did not contain complete explanations for every component of the FGSM formula.

This demonstrates an important property of RAG systems: **the quality of the generated answer depends directly on the quality and completeness of the retrieved context.**